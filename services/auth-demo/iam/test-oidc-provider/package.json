{
  "name": "test-oidc-provider",
  "version": "1.0.0",
  "description": "OIDC provider to be used offline and for testing",
  "main": "index.js",
  "scripts": {
    "start": "cross-env DEBUG=oidc-provider:* nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leon York",
  "license": "MIT",
  "dependencies": {
    "oidc-provider": "^6.16.1"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "nodemon": "^2.0.1"
  }
}
